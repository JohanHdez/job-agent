---
phase: 02-auth-+-users
plan: 04
type: execute
wave: 1
depends_on: ["02-02"]
files_modified:
  - apps/api/src/modules/profiles/schemas/user-profile.schema.ts
  - apps/api/src/modules/profiles/profiles.service.ts
  - apps/api/src/modules/profiles/profiles.controller.ts
  - apps/api/src/modules/profiles/profiles.module.ts
  - apps/api/src/modules/profiles/dto/patch-profile.dto.ts
  - apps/api/src/modules/profiles/profiles.service.test.ts
  - apps/api/src/app.module.ts
autonomous: true
gap_closure: true
requirements: [PROF-01, PROF-02, PROF-03, PROF-04, NF-03, NF-08]

must_haves:
  truths:
    - "POST /profiles/me/cv accepts a PDF (≤10MB), calls runCvParser, and upserts the result in the profiles MongoDB collection keyed by userId"
    - "POST /profiles/me/import-linkedin calls OIDC userinfo, attempts v2/me for headline, returns {profile, imported[], missing[]} on partial 403 — never throws on non-partner 403"
    - "GET /profiles/me returns the authenticated user's profile document (or 404 if none)"
    - "PATCH /profiles/me applies partial field updates with userId filter; returns updated document"
    - "checkCompleteness returns a string[] of missing field names when skills or experience are empty"
    - "All MongoDB queries in ProfilesService include userId filter (NF-08)"
    - "LinkedIn import resolves in < 8 seconds when HTTP calls are mocked (NF-03)"
    - "ProfilesModule is imported in app.module.ts and the profiles collection is accessible"
  artifacts:
    - path: "apps/api/src/modules/profiles/schemas/user-profile.schema.ts"
      provides: "UserProfile Mongoose schema mirroring ProfessionalProfile from packages/core"
      contains: "userId"
    - path: "apps/api/src/modules/profiles/profiles.service.ts"
      provides: "importFromLinkedin(), uploadCv(), patchProfile(), getProfile(), checkCompleteness()"
      exports: ["ProfilesService"]
    - path: "apps/api/src/modules/profiles/profiles.controller.ts"
      provides: "GET /profiles/me, PATCH /profiles/me, POST /profiles/me/cv, POST /profiles/me/import-linkedin"
      exports: ["ProfilesController"]
    - path: "apps/api/src/modules/profiles/profiles.module.ts"
      provides: "ProfilesModule wiring UserProfile model + ProfilesService + ProfilesController"
      exports: ["ProfilesModule"]
    - path: "apps/api/src/app.module.ts"
      provides: "ProfilesModule registered in AppModule imports"
      contains: "ProfilesModule"
    - path: "apps/api/src/modules/profiles/profiles.service.test.ts"
      provides: "7 passing tests replacing it.todo stubs for PROF-01 through NF-08"
      contains: "importFromLinkedin"
  key_links:
    - from: "apps/api/src/modules/profiles/profiles.controller.ts"
      to: "apps/api/src/modules/profiles/profiles.service.ts"
      via: "ProfilesController injects ProfilesService"
      pattern: "ProfilesService"
    - from: "apps/api/src/modules/profiles/profiles.service.ts"
      to: "apps/api/src/modules/profiles/schemas/user-profile.schema.ts"
      via: "MongoDB findOneAndUpdate with { userId } filter always first"
      pattern: "\\{ userId \\}"
    - from: "apps/api/src/modules/profiles/profiles.service.ts"
      to: "@job-agent/cv-parser"
      via: "runCvParser() called with temp file path"
      pattern: "runCvParser"
    - from: "apps/api/src/app.module.ts"
      to: "apps/api/src/modules/profiles/profiles.module.ts"
      via: "ProfilesModule in AppModule.imports"
      pattern: "ProfilesModule"
---

<objective>
Build the complete ProfilesModule: UserProfileSchema, ProfilesService (CV upload, LinkedIn import with graceful degradation, patch, completeness check), ProfilesController (4 REST endpoints), and wire it into AppModule.

Purpose: Closes the largest gap cluster from verification — 7 it.todo stubs and 4 missing files (PROF-01 through PROF-04, NF-03, NF-08). Without this, the agent has no professional profile data to use for job scoring in Phase 4.
Output: A fully tested ProfilesModule with the profiles MongoDB collection operational.
</objective>

<execution_context>
@C:/Users/INVITADOS FAMILIA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/INVITADOS FAMILIA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-auth-+-users/02-CONTEXT.md
@.planning/phases/02-auth-+-users/02-RESEARCH.md
@.planning/phases/02-auth-+-users/02-01-SUMMARY.md
@.planning/phases/02-auth-+-users/02-02-SUMMARY.md

<interfaces>
<!-- Existing types and patterns the executor needs — do not redefine -->

From packages/core/src/types/cv.types.ts:
```typescript
export interface ProfessionalProfile {
  fullName: string;
  email: string;
  phone?: string;
  location?: string;
  linkedinUrl?: string;
  headline: string;
  summary: string;
  seniority: 'Junior' | 'Mid' | 'Senior' | 'Lead' | 'Principal' | 'Executive';
  yearsOfExperience: number;
  skills: string[];
  techStack: string[];
  languages: Language[];
  experience: WorkExperience[];
  education: Education[];
}
export interface WorkExperience { company: string; title: string; startDate: string; endDate: string; description: string[]; technologies: string[]; }
export interface Education { institution: string; degree: string; field: string; graduationYear: number; }
export interface Language { name: string; level: 'Native' | 'Fluent' | 'Advanced' | 'Intermediate' | 'Basic'; }
```

From packages/cv-parser/src/index.ts (the export to call):
```typescript
// Import as: import { runCvParser } from '@job-agent/cv-parser';
export async function runCvParser(filePath: string): Promise<ProfessionalProfile>
```

From apps/api/src/modules/users/users.service.ts:
```typescript
async findById(id: string): Promise<UserDocument | null>
// Use this to fetch the encrypted LinkedIn access token before calling LinkedIn API
// User.linkedinAccessToken is the encrypted token (AES-256-GCM format)
```

From apps/api/src/common/crypto/token-cipher.ts:
```typescript
export function encryptToken(plaintext: string): string  // returns "iv:authTag:ciphertext"
export function decryptToken(ciphertext: string): string  // decrypts "iv:authTag:ciphertext"
// Use decryptToken(user.linkedinAccessToken) before making LinkedIn API calls
```

Logger injection pattern (from apps/api/src/modules/logger/logger.constants.ts):
```typescript
import { LOGGER } from '../logger/logger.constants.js';
// @Inject(LOGGER) private readonly logger: Logger (from 'winston')
```

JWT payload / authenticated request:
```typescript
// req.user.sub = MongoDB user _id as string (the userId for all DB queries)
// Type the controller param as: @Req() req: Request & { user: { sub: string } }
```

Existing infrastructure in app.module.ts:
```typescript
// Already imported: ConfigModule, MongooseModule.forRootAsync, LoggerModule,
// HealthModule, UsersModule, AuthModule
// Add ProfilesModule to this imports array
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UserProfileSchema + ProfilesService with tests</name>
  <files>
    apps/api/src/modules/profiles/schemas/user-profile.schema.ts,
    apps/api/src/modules/profiles/profiles.service.ts,
    apps/api/src/modules/profiles/dto/patch-profile.dto.ts,
    apps/api/src/modules/profiles/profiles.service.test.ts
  </files>
  <behavior>
    - importFromLinkedin(userId, encryptedToken): calls OIDC userinfo endpoint; on success populates fullName+email+imported[]; wraps v2/me and any extended calls in try/catch (403 → push field to missing[], never throw); returns { profile: UserProfileDocument, imported: string[], missing: string[] }
    - importFromLinkedin resolves in < 8000ms when HTTP is mocked (NF-03)
    - uploadCv(userId, buffer): writes buffer to /tmp/cv-{userId}-{timestamp}.pdf, calls runCvParser(tmpPath), upserts result, deletes tmp file in finally block; returns UserProfileDocument
    - patchProfile(userId, dto): findOneAndUpdate with { userId } as first filter arg, $set dto fields, upsert: false, new: true; throws NotFoundException if no profile exists
    - getProfile(userId): findOne({ userId }); returns document or null (controller handles 404)
    - checkCompleteness(profile): returns string[] of missing field names — checks skills.length, yearsOfExperience, experience.length; returns [] when all critical fields present
    - Every query method: { userId } always the first argument to findOne/findOneAndUpdate
    - upsertProfile(userId, partial): internal helper — findOneAndUpdate with { userId }, $set partial, upsert: true, new: true; used by both importFromLinkedin and uploadCv
  </behavior>
  <action>
    1. Create apps/api/src/modules/profiles/schemas/user-profile.schema.ts:
       - @Schema({ timestamps: true, collection: 'profiles' }) class UserProfile
       - @Prop({ required: true, index: true, unique: true }) userId: string — unique index enforced at DB level (prevents duplicate profiles on concurrent upserts per research pitfall 5)
       - All fields from ProfessionalProfile mapped as @Prop fields:
         fullName (required), email (required), phone?, location?, linkedinUrl?,
         headline (required), summary (required),
         seniority (required, enum: Junior/Mid/Senior/Lead/Principal/Executive),
         yearsOfExperience (required, number),
         skills (type: [String], default: []),
         techStack (type: [String], default: []),
         languages (type: [Object], default: []) — stores Language[] shape,
         experience (type: [Object], default: []) — stores WorkExperience[] shape,
         education (type: [Object], default: []) — stores Education[] shape
       - After SchemaFactory.createForClass: UserProfileSchema.index({ userId: 1 }, { unique: true })
       - Export: UserProfile class, UserProfileSchema, UserProfileDocument type (HydratedDocument<UserProfile>)
       - Import Language, WorkExperience, Education, ProfessionalProfile from '@job-agent/core' (the workspace package)

    2. Create apps/api/src/modules/profiles/dto/patch-profile.dto.ts:
       - PatchProfileDto with all ProfessionalProfile fields as optional using @IsOptional()
       - fullName: @IsOptional() @IsString(), email: @IsOptional() @IsEmail()
       - seniority: @IsOptional() @IsIn(['Junior','Mid','Senior','Lead','Principal','Executive'])
       - yearsOfExperience: @IsOptional() @IsNumber()
       - skills: @IsOptional() @IsArray() @IsString({ each: true })
       - techStack: @IsOptional() @IsArray() @IsString({ each: true })
       - languages, experience, education: @IsOptional() @IsArray() — object arrays (no deep validation needed for MVP)
       - No 'any' — type the arrays as Language[], WorkExperience[], Education[] imported from '@job-agent/core'

    3. Create apps/api/src/modules/profiles/profiles.service.ts:
       - @Injectable() ProfilesService
       - Constructor: @InjectModel(UserProfile.name) profileModel: Model<UserProfileDocument>, @Inject(LOGGER) logger: Logger, UsersService (for fetching encrypted token)
       - Implement all 6 methods described in <behavior> plus the private upsertProfile helper
       - LinkedIn import: use Node's built-in fetch (Node 18+) or import node-fetch — check if fetch is available in the runtime (apps/api uses Node 18+; built-in fetch preferred, no extra install)
         LinkedIn OIDC userinfo URL: https://api.linkedin.com/oidc/userinfo
         LinkedIn v2/me URL: https://api.linkedin.com/v2/me?projection=(localizedHeadline)
         Both calls use Authorization: Bearer {decryptedToken} header
         Treat any non-2xx as partial failure → push fields to missing[], do not throw
       - uploadCv: use import { writeFile, unlink } from 'node:fs/promises' and import { tmpdir } from 'node:os'; path = `${tmpdir()}/cv-${userId}-${Date.now()}.pdf` (cross-platform safe)
       - checkCompleteness: pure synchronous method, no DB call — takes profile document or null, returns string[]
       - All logger calls: this.logger.info/warn/error with structured object (correlationId not needed here — it's on the request level via interceptor)
       - JSDoc on every public method. No 'any'. No console.log.

    4. Replace all 7 it.todo() stubs in apps/api/src/modules/profiles/profiles.service.test.ts with real Jest assertions:
       - PROF-01 (partial success): mock fetch to return userinfo { name: 'Jane', email: 'j@example.com' }; mock v2/me to return { localizedHeadline: 'Engineer' }; assert imported includes 'name' and 'headline'
       - PROF-01 (403 fallback): mock OIDC userinfo success but v2/me returns 403; assert missing includes 'headline'; assert no exception thrown
       - PROF-02 (uploadCv): mock runCvParser to return a valid ProfessionalProfile; mock fs.writeFile + fs.unlink; assert profileModel.findOneAndUpdate called with { userId } as first arg
       - PROF-03 (patchProfile): mock profileModel.findOneAndUpdate to return updated doc; assert { userId } is first argument; assert $set contains dto fields
       - PROF-04 (checkCompleteness): call with profile where skills=[], experience=[]; assert ['skills', 'work experience'] in result; call with complete profile; assert []
       - NF-03 (timing): mock all HTTP; assert importFromLinkedin resolves in < 8000ms using jest.useFakeTimers or simply time the actual mock call
       - NF-08 (userId filter): for each DB method, assert that the mock was called with { userId } as first positional argument
       - Set up NestJS Test.createTestingModule with: profileModel mock (getModelToken), UsersService mock (findById returning user with encrypted token), logger mock (LOGGER token)
       - Mock runCvParser at module level: jest.mock('@job-agent/cv-parser', () => ({ runCvParser: jest.fn() }))
       - Mock fs/promises: jest.mock('node:fs/promises', () => ({ writeFile: jest.fn(), unlink: jest.fn() }))

    Run verification after implementation: cd apps/api && npm test -- --testPathPattern=profiles.service
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --testPathPattern=profiles.service --verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    All 7 it.todo() stubs in profiles.service.test.ts are passing tests. npm test exits 0.
    UserProfileSchema file exists with userId unique index. ProfilesService has all 6 public methods + upsertProfile helper.
    Every DB query in ProfilesService uses { userId } as primary filter.
  </done>
</task>

<task type="auto">
  <name>Task 2: ProfilesController + ProfilesModule + AppModule registration</name>
  <files>
    apps/api/src/modules/profiles/profiles.controller.ts,
    apps/api/src/modules/profiles/profiles.module.ts,
    apps/api/src/app.module.ts
  </files>
  <action>
    1. Create apps/api/src/modules/profiles/profiles.controller.ts:
       - @Controller('profiles') ProfilesController
       - Constructor: injects ProfilesService, @Inject(LOGGER) logger: Logger
       - All routes protected by default (JwtAuthGuard APP_GUARD is global — no @Public() here)
       - Extract userId from req.user.sub in each handler

       Routes:
       - GET /profiles/me — @Get('me') — calls profilesService.getProfile(userId); if null throws NotFoundException('Profile not found'); returns UserProfileDocument
       - PATCH /profiles/me — @Patch('me') @HttpCode(200) @UsePipes(ValidationPipe({ whitelist: true, forbidNonWhitelisted: true })) — calls profilesService.patchProfile(userId, body: PatchProfileDto); returns updated document
       - POST /profiles/me/cv — @Post('me/cv') @HttpCode(201) @UseInterceptors(FileInterceptor('file', { storage: memoryStorage(), limits: { fileSize: 10 * 1024 * 1024 }, fileFilter: (req, file, cb) => file.mimetype === 'application/pdf' ? cb(null, true) : cb(new BadRequestException('Only PDF files accepted'), false) })) — @UploadedFile() file: Express.Multer.File — throws BadRequestException if file is undefined; calls profilesService.uploadCv(userId, file.buffer); returns 201 + profile document
       - POST /profiles/me/import-linkedin — @Post('me/import-linkedin') @HttpCode(200) — calls user lookup to get encrypted token, then profilesService.importFromLinkedin(userId, encryptedToken); returns { profile, imported, missing } response object; if user has no linkedinAccessToken, throw BadRequestException('No LinkedIn account linked')
       - JSDoc on every public method. No 'any'. No console.log.

       Import: FileInterceptor from '@nestjs/platform-express', memoryStorage from 'multer', UploadedFile from '@nestjs/common'

    2. Create apps/api/src/modules/profiles/profiles.module.ts:
       - @Module({ imports: [MongooseModule.forFeature([{ name: UserProfile.name, schema: UserProfileSchema }]), UsersModule], providers: [ProfilesService], controllers: [ProfilesController] })
       - Import UsersModule to make UsersService available for injection in ProfilesService
       - Do NOT export ProfilesService (not needed by other modules in Phase 2)

    3. Update apps/api/src/app.module.ts:
       - Add ProfilesModule to the imports array (after AuthModule)
       - Add import statement: import { ProfilesModule } from './modules/profiles/profiles.module.js'

    Run: cd apps/api && npm run build 2>&1 | tail -20 (confirms tsc --noEmit passes)
    Run: cd apps/api && npm test 2>&1 | tail -20 (confirms no regressions)
  </action>
  <verify>
    <automated>cd apps/api && npm run build 2>&1 | tail -10 && npm test 2>&1 | tail -15</automated>
  </verify>
  <done>
    apps/api/src/modules/profiles/profiles.controller.ts exists with 4 routes.
    apps/api/src/modules/profiles/profiles.module.ts exists and imports UsersModule + MongooseModule.
    apps/api/src/app.module.ts imports ProfilesModule.
    npm run build exits 0 (zero TypeScript errors).
    npm test exits 0 (no regressions from wiring).
  </done>
</task>

</tasks>

<verification>
Full suite: cd apps/api && npm run test:cov 2>&1 | tail -30
TypeScript: cd apps/api && npm run build 2>&1 | tail -10

Expected:
- All 7 previously-todo tests in profiles.service.test.ts now pass
- ProfilesModule registered in AppModule — confirmed by grep: grep -r 'ProfilesModule' apps/api/src/app.module.ts
- UserProfileSchema has unique index on userId — confirmed by grep: grep -r 'unique: true' apps/api/src/modules/profiles/schemas/
- Every ProfilesService DB call uses userId filter — confirmed by tests asserting mock call arguments
- tsc --noEmit exits 0
- Overall test coverage >= 70%
</verification>

<success_criteria>
- PROF-01: POST /profiles/me/import-linkedin returns { profile, imported: ['name', 'email', ...], missing: ['skills', ...] } — never throws on LinkedIn 403
- PROF-02: POST /profiles/me/cv accepts multipart PDF, calls runCvParser, upserts profile, returns 201 + profile document
- PROF-03: PATCH /profiles/me accepts partial profile fields, persists via findOneAndUpdate with { userId } filter, returns 200 + updated document
- PROF-04: checkCompleteness(['skills', 'work experience']) fires on GET /auth/me when profile lacks critical fields (implementation of alert hook deferred to AuthService.getMe() — add note that AUTH-04 /auth/me should call checkCompleteness and include missingFields in response)
- NF-03: importFromLinkedin resolves in < 8000ms when HTTP calls are mocked (test confirms timing)
- NF-08: All ProfilesService queries confirmed to use { userId } as primary filter argument
- tsc --noEmit exits 0, npm test exits 0, coverage >= 70%
</success_criteria>

<output>
After completion, create .planning/phases/02-auth-+-users/02-04-SUMMARY.md following the standard summary template.
</output>
