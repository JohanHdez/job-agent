---
phase: 01-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/dist/
  - package.json
autonomous: true
requirements:
  - NF-06
gap_closure: true

must_haves:
  truths:
    - "npm run dev:services boots both packages/api (Express) and apps/api (NestJS) without TS2305 errors"
    - "packages/core/dist/ contains up-to-date declarations for SsePayload and SseProgressPayload"
    - "The dev:services script rebuilds packages/core before starting watchers so stale dist never blocks startup"
  artifacts:
    - path: "packages/core/dist/index.d.ts"
      provides: "Re-exported barrel including all job.types.ts SSE types"
      contains: "SsePayload"
    - path: "package.json"
      provides: "Updated dev:services script that builds core before starting watchers"
      contains: "build -w packages/core"
  key_links:
    - from: "package.json dev:services script"
      to: "packages/core/dist/"
      via: "npm run build -w packages/core runs before concurrently"
      pattern: "build.*packages/core.*&&.*concurrently"
    - from: "packages/api/src/routes/agent.routes.ts"
      to: "packages/core/dist/types/job.types.d.ts"
      via: "import { SsePayload, SseProgressPayload } from '@job-agent/core'"
      pattern: "SsePayload"
---

<objective>
Fix the stale packages/core/dist/ that causes TS2305 errors ("Module '@job-agent/core' has no exported member 'SsePayload'") when running npm run dev:services, and make the dev:services script rebuild core before starting watchers so this cannot recur.

Purpose: UAT tests 1 and 2 are blocked — the NestJS API cannot boot because the legacy Express gateway (packages/api) fails TypeScript compilation against stale dist output from packages/core. Tests 3 and 7 are skipped for the same reason.

Output: Rebuilt packages/core/dist/ with current type declarations; updated dev:services script that builds dependencies first.
</objective>

<execution_context>
@C:/Users/1234/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1234/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<interfaces>
<!-- The error originates in the legacy Express API gateway -->

From packages/api/src/routes/agent.routes.ts (lines 20-21):
```typescript
import type {
  SsePayload,
  SseProgressPayload,
} from '@job-agent/core';
```

From packages/core/src/types/job.types.ts (lines 140-156) — source is CORRECT:
```typescript
export interface SseProgressPayload extends SseBasePayload { ... }
export type SsePayload = SseJobFoundPayload | SseJobAppliedPayload | ... | SseProgressPayload;
```

From packages/core/src/types/index.ts — barrel is CORRECT:
```typescript
export * from './job.types.js';
```

Current root package.json dev:services script (the problem):
```json
"dev:services": "concurrently \"npm run dev -w packages/api\" \"npm run dev -w @job-agent/api\""
```
This starts watchers immediately without building packages/core first.
The packages/api tsconfig.json uses project references to ../core, so it resolves
against packages/core/dist/ — which is stale if core was never built or was built
before the SSE types were added.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild packages/core and fix dev:services to build dependencies first</name>
  <files>packages/core/dist/, package.json</files>
  <action>
    Two changes to permanently resolve the stale-dist TS2305 error:

    1. Rebuild packages/core dist output:
       Run: cd packages/core && npm run build
       This regenerates dist/ from current source, including SsePayload and SseProgressPayload declarations.
       Verify the built output contains the types:
       grep "SsePayload" packages/core/dist/types/job.types.d.ts

    2. Update root package.json "dev:services" script to build packages/core before starting watchers:
       Change from:
         "dev:services": "concurrently \"npm run dev -w packages/api\" \"npm run dev -w @job-agent/api\""
       To:
         "dev:services": "npm run build -w packages/core && concurrently \"npm run dev -w packages/api\" \"npm run dev -w @job-agent/api\""

       This ensures packages/core/dist/ is always fresh before any downstream watcher starts.
       The build takes ~2 seconds so it does not meaningfully slow down dev startup.

    3. Also update the root "typecheck" script if it does not already include packages/core build
       (it does — confirmed from Plan 01: the typecheck script starts with "npm run build -w packages/core").
       No change needed for typecheck.

    4. Verify the fix works end-to-end:
       Run: npm run typecheck
       Expected: exits 0 with no TS2305 errors for SsePayload or SseProgressPayload.
  </action>
  <verify>
    <automated>cd C:/Users/1234/OneDrive/Escritorio/claude/job-agent && npm run build -w packages/core && node -e "const fs = require('fs'); const d = fs.readFileSync('packages/core/dist/types/job.types.d.ts', 'utf8'); console.assert(d.includes('SsePayload'), 'SsePayload missing from dist'); console.assert(d.includes('SseProgressPayload'), 'SseProgressPayload missing from dist'); console.log('PASS: SSE types present in packages/core/dist')" && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    - packages/core/dist/ contains current declarations including SsePayload and SseProgressPayload
    - npm run typecheck exits 0 with no TS2305 errors
    - dev:services script in root package.json builds packages/core before starting watchers
  </done>
</task>

</tasks>

<verification>
1. grep -c "SsePayload" packages/core/dist/types/job.types.d.ts returns >= 1
2. npm run typecheck exits 0
3. Root package.json dev:services script contains "npm run build -w packages/core &&" before concurrently
</verification>

<success_criteria>
- TS2305 errors for SsePayload and SseProgressPayload are eliminated
- npm run typecheck passes cleanly (exit 0)
- dev:services script rebuilds packages/core before starting watchers, preventing future stale-dist issues
- UAT tests 1 (cold start smoke) and 2 (GET /health) are unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
