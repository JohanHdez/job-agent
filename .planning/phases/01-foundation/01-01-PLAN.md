---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/nest-cli.json
  - apps/api/src/main.ts
  - apps/api/src/app.module.ts
  - apps/api/src/common/crypto/token-cipher.ts
  - apps/api/src/modules/auth/
  - apps/api/src/modules/users/
  - packages/logger/src/index.ts
  - packages/logger/tsconfig.json
  - packages/logger/package.json
  - package.json
  - tsconfig.base.json
autonomous: true
requirements:
  - NF-06
  - NF-11

must_haves:
  truths:
    - "apps/api exists as the NestJS app directory (not apps/microservices/user-service)"
    - "packages/logger/src/index.ts exists as TypeScript source (not just compiled JS)"
    - "tsc --noEmit covers apps/api and packages/logger with zero errors"
    - "TypeScript is pinned to a single exact version at root; no per-package devDependency"
    - "AES-256-GCM token cipher is present in apps/api (NF-06 preserved)"
    - "apps/api/src/app.module.ts does NOT import LoggerModule after this plan — that wiring happens in Plan 02"
  artifacts:
    - path: "apps/api/src/app.module.ts"
      provides: "Root NestJS module — ConfigModule, MongooseModule, RedisModule wired"
      contains: "AppModule"
    - path: "apps/api/src/main.ts"
      provides: "NestJS bootstrap with ValidationPipe, CORS, port from env"
    - path: "apps/api/src/common/crypto/token-cipher.ts"
      provides: "AES-256-GCM encryptToken / decryptToken"
      exports: ["encryptToken", "decryptToken"]
    - path: "packages/logger/src/index.ts"
      provides: "Winston createLogger factory + requestContext AsyncLocalStorage"
      exports: ["createLogger", "requestContext"]
    - path: "packages/logger/tsconfig.json"
      provides: "TypeScript config for logger package"
  key_links:
    - from: "packages/logger/src/index.ts"
      to: "AsyncLocalStorage"
      via: "node:async_hooks import"
      pattern: "AsyncLocalStorage"
---

<objective>
Migrate apps/microservices/user-service to apps/api, create the TypeScript source for packages/logger, and pin TypeScript to a single exact version monorepo-wide.

Purpose: Establish the correct monorepo topology (apps/api as the NestJS Modular Monolith) and ensure tsc --noEmit covers all critical packages without a version mismatch. All existing auth/users code is carried over intact — Phase 2 will complete it. The logger TypeScript source is the prerequisite for NestJS DI import in Plan 02.

Output: apps/api directory fully scaffolded from user-service, packages/logger/src/index.ts committed as TypeScript source, root package.json pinned to exact TypeScript version, apps/api and packages/logger pass tsc --noEmit.
</objective>

<execution_context>
@C:/Users/1234/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1234/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/logger/src/index.js (compiled — reverse to TypeScript source):
The logger exports createLogger(serviceName: string) returning a Winston Logger,
and requestContext as AsyncLocalStorage<{ correlationId: string; userId?: string }>.
The createLogger factory reads correlationId and userId from requestContext in the
default log formatter.

From apps/microservices/user-service/src/app.module.ts (carry forward as-is):
ConfigModule.forRoot({ isGlobal: true }) pattern
MongooseModule.forRootAsync({ useFactory: () => ({ uri: process.env['MONGO_API_URI'] }) })

From apps/microservices/user-service/src/common/crypto/token-cipher.ts:
export function encryptToken(plaintext: string): string  // AES-256-GCM, base64 output
export function decryptToken(ciphertext: string): string  // AES-256-GCM decrypt

Key env vars to reference in .env.example update:
  MONGO_API_URI=mongodb://localhost:27017/job-agent-api
  REDIS_URL=redis://localhost:6379
  API_PORT=3001
  JWT_SECRET=change-me-in-production
  TOKEN_CIPHER_KEY=32-byte-hex-string
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate user-service to apps/api</name>
  <files>
    apps/api/package.json,
    apps/api/tsconfig.json,
    apps/api/nest-cli.json,
    apps/api/src/main.ts,
    apps/api/src/app.module.ts,
    apps/api/src/common/crypto/token-cipher.ts,
    apps/api/src/modules/auth/ (entire directory — carry over),
    apps/api/src/modules/users/ (entire directory — carry over),
    package.json (root — update workspaces and scripts)
  </files>
  <action>
    Copy the full contents of apps/microservices/user-service into apps/api. This is a directory copy, not a scaffold from scratch.

    After copying:

    1. Update apps/api/package.json:
       - Change name from "@job-agent/user-service" to "@job-agent/api"
       - Remove "typescript" from devDependencies (root handles it)
       - Add "@job-agent/logger": "*" to dependencies
       - Add ioredis: "^5.x" to dependencies (Redis client for health check)
       - Add @nestjs/terminus: "^10.x" to dependencies
       - Add jest, ts-jest, @types/jest, supertest, @types/supertest to devDependencies
       - Update scripts: "test" script must be "jest --passWithNoTests", "test:cov" must be "jest --coverage --passWithNoTests"

    2. Update apps/api/tsconfig.json:
       - Keep "extends": "../../tsconfig.base.json"
       - Keep "module": "commonjs", "moduleResolution": "node", "experimentalDecorators": true, "emitDecoratorMetadata": true
       - Paths must include "@job-agent/logger": ["../../packages/logger/src/index.ts"]

    3. Update apps/api/src/main.ts:
       - Change port env var from whatever it was to process.env['API_PORT'] ?? '3001'
       - Replace any process.stdout.write or console.log calls with createLogger('api') from @job-agent/logger
       - Import createLogger directly in main.ts (cannot use @Inject in bootstrap function)

    4. Update apps/api/src/app.module.ts:
       - Change MONGO_USER_SERVICE_URI references to MONGO_API_URI
       - DO NOT add LoggerModule or HealthModule here yet — that is Plan 02's job
       - DO NOT add APP_GUARD here yet — that is Plan 02's job
       - Keep UsersModule and AuthModule imports as-is (Phase 2 will complete them)

    5. Keep apps/api/src/common/crypto/token-cipher.ts exactly as-is — it implements NF-06 (AES-256-GCM); do not modify.

    6. Update root package.json:
       - Add "apps/api" to workspaces (alongside "apps/microservices/*" — keep both during transition)
       - Update "typecheck" script to include "tsc --noEmit -p apps/api/tsconfig.json"
       - Update "dev:services" script to reference apps/api instead of user-service

    Note: Do NOT delete apps/microservices/user-service — leave it in place. The migration is additive. Root workspaces will include both temporarily.

    Note: Update .env.example to add MONGO_API_URI and API_PORT entries, mark MONGO_USER_SERVICE_URI as deprecated.
  </action>
  <verify>
    <automated>cd C:/Users/1234/OneDrive/Escritorio/claude/job-agent && node -e "const p = require('./apps/api/package.json'); console.assert(p.name === '@job-agent/api', 'wrong name'); console.assert(!p.devDependencies?.typescript, 'typescript should not be in devDeps'); console.log('package.json OK')" && test -f apps/api/src/main.ts && echo "main.ts OK" && test -f apps/api/src/app.module.ts && echo "app.module.ts OK" && test -f apps/api/src/common/crypto/token-cipher.ts && echo "token-cipher.ts OK" && test -d apps/api/src/modules/auth && echo "auth/ OK" && test -d apps/api/src/modules/users && echo "users/ OK"</automated>
  </verify>
  <done>
    apps/api/package.json has name "@job-agent/api", no per-package typescript devDependency, and includes @nestjs/terminus, ioredis, @job-agent/logger in dependencies. apps/api/src/app.module.ts references MONGO_API_URI. auth/ and users/ directories are present and unchanged from user-service. main.ts, app.module.ts, and token-cipher.ts all exist at their expected paths.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create packages/logger TypeScript source</name>
  <files>
    packages/logger/src/index.ts,
    packages/logger/tsconfig.json,
    packages/logger/package.json
  </files>
  <behavior>
    - createLogger('api') returns a Winston logger with 'api' as service name in log metadata
    - requestContext.getStore() returns { correlationId, userId } when called within a requestContext.run() block
    - requestContext.getStore() returns undefined when called outside a requestContext.run() block
    - Logger formats include correlationId from requestContext.getStore() when present
    - Logger formats include userId from requestContext.getStore() when present, omits it when undefined
  </behavior>
  <action>
    Create packages/logger/src/index.ts as a proper TypeScript source file. Derive the implementation from the existing packages/logger/src/index.js compiled output — the compiled JS is the spec; create the .ts source that would produce it.

    The TypeScript source must:
    - Import AsyncLocalStorage from 'node:async_hooks'
    - Import winston (Logger, createLogger as winstonCreateLogger, format, transports)
    - Export requestContext as AsyncLocalStorage&lt;{ correlationId: string; userId?: string }&gt;
    - Export createLogger(serviceName: string): Logger that:
      - Uses winston.format.combine(format.timestamp(), format.json())
      - In the format, reads requestContext.getStore() to attach correlationId and userId to each log entry
      - Adds metadata: { service: serviceName }
      - Uses Console transport in all environments (simple format in dev, JSON in production)
    - No chalk dependency — plain JSON format only (as per research note)
    - All functions and exports must have JSDoc comments
    - No 'any' types — use proper Winston types

    Update packages/logger/package.json:
    - Remove "main": "src/index.js" if present, add "main": "dist/index.js"
    - Add "types": "dist/index.d.ts"
    - Ensure "scripts" includes "build": "tsc -p tsconfig.json"
    - Ensure dependencies include "winston": "^3.13.0"

    Create packages/logger/tsconfig.json:
    ```json
    {
      "extends": "../../tsconfig.base.json",
      "compilerOptions": {
        "module": "commonjs",
        "moduleResolution": "node",
        "outDir": "./dist",
        "rootDir": "./src"
      },
      "include": ["src/**/*.ts"],
      "exclude": ["dist", "node_modules"]
    }
    ```

    Note: The existing packages/logger/src/index.d.ts is the ground truth for the exported types — the .ts source must match those declarations exactly.
  </action>
  <verify>
    <automated>cd C:/Users/1234/OneDrive/Escritorio/claude/job-agent && npx tsc --noEmit -p packages/logger/tsconfig.json</automated>
  </verify>
  <done>
    packages/logger/src/index.ts exists as a TypeScript file. tsc --noEmit -p packages/logger/tsconfig.json exits with code 0. File exports createLogger and requestContext with proper TypeScript types matching the existing .d.ts declarations.
  </done>
</task>

<task type="auto">
  <name>Task 3: Pin TypeScript monorepo-wide to single exact version</name>
  <files>
    package.json (root),
    apps/api/package.json,
    apps/microservices/user-service/package.json
  </files>
  <action>
    1. Determine the latest stable TypeScript 5.x version:
       Run: npm show typescript@latest version
       Use that exact version string (e.g., "5.8.3") for all subsequent steps.

    2. Update root package.json devDependencies:
       - Change "typescript": "^5.4.5" to "typescript": "{EXACT_VERSION}" (exact pin, no ^ or ~)

    3. Remove "typescript" from devDependencies in:
       - apps/api/package.json (if present after Task 1)
       - apps/microservices/user-service/package.json (if present)
       - Any other workspace package.json that has typescript as a devDependency

       Check all workspace package.json files with:
       grep -r '"typescript"' apps/ packages/ --include="package.json"

    4. Run npm install from the workspace root to resolve hoisting:
       npm install

    5. Verify TypeScript resolves to a single version:
       npm ls typescript

    6. Run the monorepo typecheck to confirm zero errors:
       npm run typecheck
       (This runs tsc --noEmit across all configured workspaces including apps/api after Task 1's root script update)

    Note: tsconfig.base.json "target" and "module" resolution are NOT changed — leave "ES2022" and "Node16" as-is per the locked decision.
  </action>
  <verify>
    <automated>cd C:/Users/1234/OneDrive/Escritorio/claude/job-agent && npm ls typescript 2>/dev/null | grep -v "deduped" | grep "typescript@" | wc -l | xargs -I{} test {} -eq 1 && echo "Single TypeScript version OK" || echo "MULTIPLE TYPESCRIPT VERSIONS DETECTED"</automated>
  </verify>
  <done>
    npm ls typescript shows exactly one TypeScript version installed (no duplicates from workspace-local installs). Root package.json has typescript pinned to an exact version (e.g., "5.8.3") with no ^ or ~ prefix. No individual workspace package.json contains typescript in devDependencies.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. apps/api directory exists with correct package.json (name: "@job-agent/api", no TS devDep, has terminus + ioredis + logger deps)
2. packages/logger/src/index.ts exists and passes: npx tsc --noEmit -p packages/logger/tsconfig.json
3. npm ls typescript shows a single version
4. apps/microservices/user-service is untouched (still present)
5. apps/api/src/common/crypto/token-cipher.ts present (NF-06)
6. apps/api/src/app.module.ts does NOT import LoggerModule (that is Plan 02's responsibility)
</verification>

<success_criteria>
- apps/api/ exists with all user-service source code migrated
- packages/logger/src/index.ts is a valid TypeScript file exporting createLogger and requestContext
- tsc --noEmit on packages/logger/tsconfig.json exits 0
- npm ls typescript shows exactly one version in the dependency tree
- No workspace package.json has typescript in devDependencies
- apps/api/src/common/crypto/token-cipher.ts exists unchanged (AES-256-GCM for NF-06)
- apps/api/src/app.module.ts does NOT yet import LoggerModule (Plan 02 handles that wiring)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` covering:
- Exact TypeScript version pinned
- apps/api directory confirmed (list key files)
- packages/logger/src/index.ts exports confirmed
- Any deviations from plan and reasons
- Token cipher location confirmed (NF-06)
</output>
