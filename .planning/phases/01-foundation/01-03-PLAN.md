---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .github/workflows/ci.yml
  - package.json
autonomous: true
requirements:
  - NF-09
  - NF-10
  - NF-11
  - NF-17

must_haves:
  truths:
    - "Pushing to main or develop triggers the CI workflow on GitHub Actions"
    - "CI fails if tsc --noEmit returns any TypeScript errors (NF-11)"
    - "CI fails if Jest coverage for apps/api drops below 70% (NF-10)"
    - "CI fails if gitleaks detects a secret in the pushed diff (NF-09)"
    - "CI runs all three gates in parallel (typecheck, test, secrets) — no sequential blocking"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI pipeline with typecheck + Jest coverage + gitleaks jobs"
      contains: "gitleaks-action"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json typecheck script"
      via: "npm run typecheck in CI typecheck job"
      pattern: "npm run typecheck"
    - from: ".github/workflows/ci.yml"
      to: "apps/api jest.config.ts"
      via: "npm run test:cov -w @job-agent/api in CI test job"
      pattern: "test:cov"
    - from: ".github/workflows/ci.yml"
      to: "zricethezav/gitleaks-action@v2"
      via: "uses: in secrets job"
      pattern: "gitleaks-action"
---

<objective>
Replace the existing .github/workflows/ci.yml with a clean pipeline that enforces the three Phase 1 gates: TypeScript zero-error compile, Jest coverage >= 70% for apps/api, and gitleaks secret scanning on the push diff.

Purpose: CI is the enforcement layer for NF-09 (no secrets in repo), NF-10 (coverage gate), NF-11 (TypeScript strict), and NF-17 (automated pipeline on every push). Without this, the gates exist only as local commands — CI makes them mandatory on every push to main and develop.

Output: .github/workflows/ci.yml with three parallel jobs (typecheck, test, secrets). Push to develop triggers all three. The pipeline references the typecheck and test:cov scripts established in Plans 01 and 02.
</objective>

<execution_context>
@C:/Users/1234/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1234/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Current CI file structure to replace -->

The existing .github/workflows/ci.yml has jobs that need to be replaced entirely.
The following scripts must exist in root package.json (confirmed by Plans 01-02):
  "typecheck" — runs tsc --noEmit across apps/api and packages
  "test:cov" does NOT exist at root; each workspace runs its own

apps/api/package.json scripts (from Plan 02):
  "test:cov": "jest --coverage --passWithNoTests"

The packages that must be built before typecheck (packages/core and packages/logger
export types that apps/api imports — tsc --noEmit on apps/api needs their declarations):
  npm run build -w packages/core
  npm run build -w packages/logger

Key constraint from RESEARCH.md:
  gitleaks uses 'protect' mode by default (scans diff of the push, not full history).
  Use zricethezav/gitleaks-action@v2 with GITHUB_TOKEN — zero additional config needed.
  fetch-depth: 0 is required for gitleaks to access the full push diff.

From CONTEXT.md locked decisions:
  - CI triggers: push and pull_request targeting main and develop branches
  - No ESLint gate in Phase 1
  - NF-17: merge to main auto-deploys to staging; production deploy requires manual approval
    (deploy-staging.yml and deploy-prod.yml already exist — ci.yml does NOT handle deploys,
    only the gate checks)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CI pipeline with typecheck + Jest + gitleaks gates</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <action>
    Replace the entire contents of .github/workflows/ci.yml with the following pipeline structure.
    Do NOT preserve any existing content — the replacement is clean.

    The pipeline must have:

    Triggers:
    - on.push.branches: [main, develop]
    - on.pull_request.branches: [main, develop]

    Concurrency:
    - group: ci-${{ github.ref }}
    - cancel-in-progress: true

    Three parallel jobs:

    Job 1 — typecheck (TypeScript zero errors):
    - name: "TypeScript (zero errors)"
    - runs-on: ubuntu-latest
    - steps:
      1. actions/checkout@v4
      2. actions/setup-node@v4 with node-version: '20' and cache: 'npm'
      3. npm ci
      4. npm run build -w packages/core   (generates types needed by apps/api)
      5. npm run build -w packages/logger  (generates types needed by apps/api)
      6. npm run typecheck
         (This runs tsc --noEmit across apps/api and other workspaces per the root script)

    Job 2 — test (Jest coverage >= 70%):
    - name: "Jest (coverage >= 70%)"
    - runs-on: ubuntu-latest
    - steps:
      1. actions/checkout@v4
      2. actions/setup-node@v4 with node-version: '20' and cache: 'npm'
      3. npm ci
      4. npm run build -w packages/core
      5. npm run build -w packages/logger
      6. npm run test:cov -w @job-agent/api
         (Runs jest --coverage with 70% threshold — exits non-zero if below threshold)

    Job 3 — secrets (gitleaks):
    - name: "Secret scan (gitleaks)"
    - runs-on: ubuntu-latest
    - steps:
      1. actions/checkout@v4 with fetch-depth: 0
         (fetch-depth: 0 required for gitleaks to access push diff)
      2. uses: zricethezav/gitleaks-action@v2
         env:
           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
         (Default 'protect' mode — scans only the diff of the current push, not history)

    Notes:
    - No ESLint job (deferred per locked decision)
    - No deploy steps in ci.yml (deploy-staging.yml and deploy-prod.yml handle deploys separately)
    - All three jobs run independently (no needs: dependency between them) — parallel execution
    - Node 20 is required (crypto.randomUUID() is built-in from Node 15+; Node 20 is LTS)

    After writing the file, validate the YAML syntax locally:
    node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml', 'utf8')); console.log('YAML valid')"
    (js-yaml is available as a transitive dependency)
  </action>
  <verify>
    <automated>cd C:/Users/1234/OneDrive/Escritorio/claude/job-agent && node -e "const yaml = require('js-yaml'); const fs = require('fs'); const c = yaml.load(fs.readFileSync('.github/workflows/ci.yml', 'utf8')); const jobs = Object.keys(c.jobs); console.log('Jobs:', jobs); console.assert(jobs.includes('typecheck') || jobs.some(j => j.includes('type')), 'no typecheck job'); console.assert(jobs.some(j => c.jobs[j].steps?.some(s => s.uses?.includes('gitleaks'))), 'no gitleaks step'); console.log('CI YAML valid')" 2>&amp;1</automated>
  </verify>
  <done>
    .github/workflows/ci.yml is valid YAML. It contains three jobs: typecheck (runs tsc), test (runs jest --coverage), and secrets (uses gitleaks-action@v2). Triggers are push and pull_request on main and develop. No ESLint job. No deploy steps.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full Phase 1 CI pipeline is written. All three PLAN.md execution tasks are complete:
    - Plan 01: apps/api scaffolded from user-service, packages/logger TypeScript source created, TypeScript pinned
    - Plan 02: NestJS infrastructure modules wired (Health, Logger, JwtGuard, CorrelationInterceptor), Jest tests passing with >= 70% coverage
    - Plan 03: .github/workflows/ci.yml replaced with typecheck + Jest + gitleaks pipeline
  </what-built>
  <how-to-verify>
    Step 1 — Verify apps/api exists and NestJS boots:
    From the repo root: npm install (ensure dependencies resolved)
    From apps/api: npm run build 2>&amp;1 | tail -10
    Expected: NestJS build completes without errors

    Step 2 — Verify GET /health:
    In a separate terminal: npm run dev -w @job-agent/api
    Wait for "Application is running on port 3001" log
    curl http://localhost:3001/health
    Expected: HTTP 200 with JSON containing { "status": "ok", "uptime": &lt;number&gt;, "version": "1.0.0" }
    (Note: MongoDB must be running locally or MONGO_API_URI must point to a reachable instance)

    Step 3 — Verify 401 on protected routes:
    curl http://localhost:3001/api/some-nonexistent-route
    Expected: HTTP 401 (not 404 — the guard fires before routing when no JWT)

    Step 4 — Verify local test suite:
    npm run test:cov -w @job-agent/api
    Expected: All 3 test files pass, coverage >= 70% shown in report

    Step 5 — Verify TypeScript compiles clean:
    npm run typecheck
    Expected: exits 0, no errors printed

    Step 6 — Verify CI pipeline YAML:
    Review .github/workflows/ci.yml in the repo
    Confirm: 3 jobs (typecheck, test, secrets), triggers on main+develop, gitleaks step present
  </how-to-verify>
  <resume-signal>Type "approved" if all verifications pass, or describe which step failed and what output you saw</resume-signal>
</task>

</tasks>

<verification>
CI pipeline verification (automated portion):
- .github/workflows/ci.yml is valid YAML with 3 jobs
- gitleaks-action@v2 step is present in the secrets job
- typecheck job includes npm run build for packages/core and packages/logger before tsc
- test job runs npm run test:cov -w @job-agent/api

Full verification requires pushing to develop and observing GitHub Actions run (manual checkpoint above).
</verification>

<success_criteria>
- .github/workflows/ci.yml exists with three parallel jobs: typecheck, test, secrets
- Triggers: push and pull_request on [main, develop]
- gitleaks-action@v2 present in secrets job with GITHUB_TOKEN env var
- typecheck job builds packages/core and packages/logger before running tsc --noEmit
- test job runs jest --coverage with 70% threshold for apps/api
- No ESLint gate (deferred per locked decision)
- Human checkpoint passed: GET /health returns 200, 401 on unprotected routes without JWT, all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` covering:
- CI YAML structure (job names and key steps)
- Checkpoint result (what was verified and by whom)
- Any issues found during manual verification and how they were resolved
- Phase 1 overall completion status
</output>
