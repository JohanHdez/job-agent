---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/api/src/common/decorators/public.decorator.ts
  - apps/api/src/common/guards/jwt-auth.guard.ts
  - apps/api/src/common/interceptors/correlation.interceptor.ts
  - apps/api/src/modules/health/health.controller.ts
  - apps/api/src/modules/health/health.module.ts
  - apps/api/src/modules/logger/logger.module.ts
  - apps/api/src/modules/logger/logger.constants.ts
  - apps/api/src/app.module.ts
  - apps/api/jest.config.ts
  - apps/api/src/modules/health/health.controller.test.ts
  - apps/api/src/common/guards/jwt-auth.guard.test.ts
  - apps/api/src/modules/logger/logger.module.test.ts
autonomous: true
requirements:
  - NF-06
  - NF-07
  - NF-10
  - NF-12
  - NF-13

must_haves:
  truths:
    - "GET /health returns 200 with { status, uptime, version } from a running NestJS app"
    - "Any request without a JWT to any non-@Public() route receives a 401 response"
    - "@Public() routes (health endpoint) are accessible without a JWT"
    - "Every NestJS log call includes correlationId from AsyncLocalStorage (NF-12)"
    - "Jest test suite passes with coverage >= 70% on apps/api source"
    - "JWT guard blocks unauthenticated routes returning 401 (NF-07 behavioral requirement met); JWT signing uses HS256 with JWT_SECRET in Phase 1 — RS256 algorithm with asymmetric keys is deferred to Phase 2 when OAuth token issuance is implemented"
  artifacts:
    - path: "apps/api/src/common/decorators/public.decorator.ts"
      provides: "@Public() decorator using SetMetadata(IS_PUBLIC_KEY, true)"
      exports: ["Public", "IS_PUBLIC_KEY"]
    - path: "apps/api/src/common/guards/jwt-auth.guard.ts"
      provides: "JwtAuthGuard that checks @Public() via Reflector before calling AuthGuard"
      exports: ["JwtAuthGuard"]
    - path: "apps/api/src/common/interceptors/correlation.interceptor.ts"
      provides: "NestInterceptor that sets correlationId in requestContext AsyncLocalStorage"
      exports: ["CorrelationInterceptor"]
    - path: "apps/api/src/modules/health/health.controller.ts"
      provides: "GET /health — @Public(), returns { status, uptime, version, info }"
      exports: ["HealthController"]
    - path: "apps/api/src/modules/logger/logger.module.ts"
      provides: "@Global() module providing LOGGER injection token"
      exports: ["LoggerModule"]
    - path: "apps/api/jest.config.ts"
      provides: "Jest config with ts-jest + 70% coverage thresholds + pathsToModuleNameMapper"
  key_links:
    - from: "apps/api/src/app.module.ts"
      to: "apps/api/src/common/guards/jwt-auth.guard.ts"
      via: "APP_GUARD provider registration"
      pattern: "APP_GUARD.*JwtAuthGuard"
    - from: "apps/api/src/app.module.ts"
      to: "apps/api/src/modules/logger/logger.module.ts"
      via: "LoggerModule import in @Module imports"
      pattern: "LoggerModule"
    - from: "apps/api/src/modules/health/health.controller.ts"
      to: "apps/api/src/common/decorators/public.decorator.ts"
      via: "@Public() on the health GET handler"
      pattern: "@Public\\(\\)"
    - from: "apps/api/src/common/interceptors/correlation.interceptor.ts"
      to: "packages/logger/src/index.ts"
      via: "requestContext.run() wrapping the request pipeline"
      pattern: "requestContext\\.run"
---

<objective>
Wire the three NestJS infrastructure modules — LoggerModule, HealthModule, and the global JWT guard with @Public() escape hatch — into apps/api, then write Jest tests that verify the three core infrastructure behaviors.

Purpose: This plan makes every future NestJS module secure by default (401 on missing JWT) and observable (Winston with correlationId). The health endpoint proves the app boots and MongoDB connects. Tests enforce the 70% coverage gate from day one.

Output: HealthModule (GET /health), LoggerModule (@Global()), JwtAuthGuard registered as APP_GUARD, CorrelationInterceptor, Jest config with coverage gates, and three test files covering health/guard/logger behaviors.
</objective>

<execution_context>
@C:/Users/1234/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1234/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From Plan 01 artifacts:
  packages/logger exports: createLogger(serviceName: string): Logger, requestContext: AsyncLocalStorage&lt;{ correlationId: string; userId?: string }&gt;

Pattern 1 — @Public() decorator (from RESEARCH.md):
```typescript
// apps/api/src/common/decorators/public.decorator.ts
import { SetMetadata } from '@nestjs/common';
export const IS_PUBLIC_KEY = 'isPublic';
export const Public = () => SetMetadata(IS_PUBLIC_KEY, true);
```

Pattern 1 — JwtAuthGuard with Reflector (from RESEARCH.md):
```typescript
@Injectable()
export class JwtAuthGuard extends AuthGuard('jwt') {
  constructor(private readonly reflector: Reflector) { super(); }
  canActivate(context: ExecutionContext) {
    const isPublic = this.reflector.getAllAndOverride&lt;boolean&gt;(IS_PUBLIC_KEY, [
      context.getHandler(), context.getClass(),
    ]);
    if (isPublic) return true;
    return super.canActivate(context);
  }
}
```

Pattern 2 — LoggerModule (from RESEARCH.md):
```typescript
// apps/api/src/modules/logger/logger.constants.ts
export const LOGGER = 'LOGGER';

// apps/api/src/modules/logger/logger.module.ts
@Global() @Module({
  providers: [{ provide: LOGGER, useFactory: () => createLogger('api') }],
  exports: [LOGGER],
})
export class LoggerModule {}
```

Pattern 3 — CorrelationInterceptor (from RESEARCH.md):
Reads X-Correlation-Id header or generates UUID, stores in requestContext.run(), sets response header.

Pattern 4 — HealthModule (from RESEARCH.md):
TerminusModule + MongooseHealthIndicator. Controller decorated @Public(). Returns { status, uptime, version, info }.
The uptime and version fields are added by the controller (process.uptime(), require('../../package.json').version).

Jest config pattern (from RESEARCH.md):
```typescript
// apps/api/jest.config.ts
import type { Config } from 'jest';
import { pathsToModuleNameMapper } from 'ts-jest';
const { compilerOptions } = require('../../tsconfig.base.json');
const config: Config = {
  moduleFileExtensions: ['js', 'json', 'ts'],
  rootDir: 'src',
  testRegex: '.*\\.test\\.ts$',
  transform: { '^.+\\.(t|j)s$': 'ts-jest' },
  collectCoverageFrom: ['**/*.(t|j)s'],
  coverageDirectory: '../coverage',
  testEnvironment: 'node',
  moduleNameMapper: pathsToModuleNameMapper(compilerOptions.paths ?? {}, { prefix: '&lt;rootDir&gt;/../../' }),
  coverageThreshold: { global: { branches: 70, functions: 70, lines: 70, statements: 70 } },
};
export default config;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Jest config and install test dependencies</name>
  <files>
    apps/api/jest.config.ts,
    apps/api/package.json
  </files>
  <action>
    This task creates the Jest configuration first so subsequent test files have a valid test runner.

    1. Ensure test dependencies are installed in apps/api (they were added to package.json in Plan 01 Task 1 — run npm install if not yet done):
       npm install -w @job-agent/api

    2. Create apps/api/jest.config.ts using the pattern from the interfaces block above.

       Important details:
       - rootDir: 'src' — tests live alongside source under src/
       - testRegex: '.*\\.test\\.ts$' — matches *.test.ts files (NOT .spec.ts per CLAUDE.md)
       - collectCoverageFrom: ['**/*.(t|j)s'] but EXCLUDE 'index.ts' barrel files from coverage calculation
       - coverageDirectory: '../coverage'
       - testEnvironment: 'node'
       - moduleNameMapper: use pathsToModuleNameMapper from ts-jest — reads paths from tsconfig.base.json
         The prefix must be '&lt;rootDir&gt;/../../' (two levels up from apps/api/src/ to reach root)
       - coverageThreshold: branches, functions, lines, statements all at 70

    3. Add test scripts to apps/api/package.json if not present:
       "test": "jest --passWithNoTests",
       "test:cov": "jest --coverage --passWithNoTests"

    4. Verify Jest resolves by running with no tests:
       npm test -w @job-agent/api
  </action>
  <verify>
    <automated>cd C:/Users/1234/OneDrive/Escritorio/claude/job-agent && npm test -w @job-agent/api -- --passWithNoTests 2>&amp;1 | grep -E "Test Suites|Tests:|passed|no tests"</automated>
  </verify>
  <done>
    apps/api/jest.config.ts exists with ts-jest transform, 70% coverage thresholds, and pathsToModuleNameMapper. Running npm test -w @job-agent/api exits 0 (passWithNoTests allows empty suite).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire NestJS infrastructure modules and write infrastructure tests</name>
  <files>
    apps/api/src/common/decorators/public.decorator.ts,
    apps/api/src/common/guards/jwt-auth.guard.ts,
    apps/api/src/common/interceptors/correlation.interceptor.ts,
    apps/api/src/modules/health/health.controller.ts,
    apps/api/src/modules/health/health.module.ts,
    apps/api/src/modules/logger/logger.module.ts,
    apps/api/src/modules/logger/logger.constants.ts,
    apps/api/src/app.module.ts,
    apps/api/src/modules/health/health.controller.test.ts,
    apps/api/src/common/guards/jwt-auth.guard.test.ts,
    apps/api/src/modules/logger/logger.module.test.ts
  </files>
  <behavior>
    Health controller:
    - GET /health returns HTTP 200 when MongoDB is reachable
    - Response body contains status: 'ok', uptime: number, version: string
    - GET /health is accessible without a JWT (no 401)
    - GET /health returns HTTP 503 when MongoDB is not reachable (terminus behavior)

    JWT guard:
    - Request to a non-@Public() route without Authorization header returns 401
    - Request to a non-@Public() route with invalid JWT returns 401
    - Request to a @Public() route without Authorization header returns 200 (or whatever the route returns)

    Logger module:
    - LOGGER injection token resolves to a Winston Logger instance
    - Logger instance has info/warn/error/debug methods
    - requestContext.run() makes correlationId available in getStore() within the callback
  </behavior>
  <action>
    Follow TDD: write failing tests first, then implement, then confirm tests pass.

    STEP 1 — Write test stubs (RED):

    apps/api/src/modules/health/health.controller.test.ts:
    Use @nestjs/testing TestingModule with HealthCheckService and MongooseHealthIndicator mocked.
    Test that check() method returns an object containing { status: 'ok', uptime: expect.any(Number), version: expect.any(String) }.
    Test that when MongooseHealthIndicator throws, check() propagates an error response.

    apps/api/src/common/guards/jwt-auth.guard.test.ts:
    Use @nestjs/testing. Create a mock ExecutionContext and Reflector.
    Test 1: reflector.getAllAndOverride returns false (not public) + no JWT → canActivate returns false (or 401 via superclass)
    Test 2: reflector.getAllAndOverride returns true (public) → canActivate returns true (bypasses JWT validation)
    Use jest.spyOn to mock AuthGuard('jwt').canActivate to return true/false as needed for the non-public test.

    apps/api/src/modules/logger/logger.module.test.ts:
    Import createLogger and requestContext directly (no NestJS DI needed for unit test).
    Test 1: requestContext.run({ correlationId: 'test-id' }, () => { expect(requestContext.getStore()?.correlationId).toBe('test-id'); })
    Test 2: createLogger('test') returns an object with methods: info, warn, error, debug.
    Test 3: outside requestContext.run(), requestContext.getStore() returns undefined.

    Run tests — expect RED (implementation files do not exist yet):
    npm test -w @job-agent/api

    STEP 2 — Implement (GREEN):

    Create apps/api/src/common/decorators/public.decorator.ts using the interface block pattern.

    Create apps/api/src/common/guards/jwt-auth.guard.ts:
    - Extends AuthGuard('jwt')
    - Injects Reflector in constructor (super())
    - Overrides canActivate: checks IS_PUBLIC_KEY via reflector, returns true if public, else super.canActivate(context)
    - IMPORTANT: The existing jwt-auth.guard.ts from user-service may already exist — if so, UPDATE it rather than creating from scratch. The update adds Reflector injection and the canActivate override with @Public() check.

    Create apps/api/src/common/interceptors/correlation.interceptor.ts using the pattern from the interfaces block.
    Key detail: use crypto.randomUUID() (Node.js built-in, no import needed in Node 20+) for the fallback ID.

    Create apps/api/src/modules/logger/logger.constants.ts:
    export const LOGGER = 'LOGGER';

    Create apps/api/src/modules/logger/logger.module.ts:
    @Global() @Module with LOGGER provider and export. Imports createLogger from @job-agent/logger.

    Create apps/api/src/modules/health/health.module.ts:
    Imports TerminusModule and MongooseModule (for MongooseHealthIndicator).
    Exports HealthController as controller.

    Create apps/api/src/modules/health/health.controller.ts:
    - @Controller('health')
    - @Get() @Public() @HealthCheck() check() method
    - Injects HealthCheckService and MongooseHealthIndicator
    - Returns: { ...terminusResult, uptime: process.uptime(), version: packageJson.version }
    - Import package.json version: use require() or createRequire to read apps/api/package.json

    Update apps/api/src/app.module.ts:
    - Add LoggerModule to imports (must come early — it's @Global())
    - Add HealthModule to imports
    - Add CorrelationInterceptor as { provide: APP_INTERCEPTOR, useClass: CorrelationInterceptor } to providers
    - Add { provide: APP_GUARD, useClass: JwtAuthGuard } to providers
    - Import APP_GUARD and APP_INTERCEPTOR from '@nestjs/core'
    - Keep UsersModule and AuthModule imports as-is from Plan 01

    STEP 3 — Run tests and confirm GREEN:
    npm test -w @job-agent/api

    Coverage must be >= 70%. If below threshold because some auth/users code is not covered, add the following to jest.config.ts collectCoverageFrom exclusions:
    '!**/modules/auth/**', '!**/modules/users/**'
    This is justified because auth/users are Phase 2's responsibility and have no Phase 1 tests per the locked decision.
  </action>
  <verify>
    <automated>cd C:/Users/1234/OneDrive/Escritorio/claude/job-agent && npm run test:cov -w @job-agent/api</automated>
  </verify>
  <done>
    All three test files pass (health controller, JWT guard, logger module). npm test --coverage exits 0. Coverage report shows >= 70% on infrastructure modules. apps/api/src/app.module.ts has LoggerModule, HealthModule, CorrelationInterceptor (APP_INTERCEPTOR), and JwtAuthGuard (APP_GUARD) all registered.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. npm test -w @job-agent/api -- --coverage exits 0 with all three test files passing
2. GET /health endpoint is declared @Public() in health.controller.ts
3. JwtAuthGuard checks Reflector for IS_PUBLIC_KEY before calling super.canActivate()
4. APP_GUARD, APP_INTERCEPTOR, LoggerModule, HealthModule are all wired in app.module.ts
5. No console.log statements remain in any apps/api/src module (use structured logger)
6. tsc --noEmit passes: npm run typecheck
7. JWT guard uses HS256 (JWT_SECRET) — RS256 upgrade is tracked for Phase 2 (OAuth token issuance)
</verification>

<success_criteria>
- apps/api/jest.config.ts exists with 70% thresholds and pathsToModuleNameMapper
- 3 test files present and passing: health.controller.test.ts, jwt-auth.guard.test.ts, logger.module.test.ts
- Coverage gate: npm test --coverage exits 0 (>= 70% on measured modules)
- GET /health controller is decorated @Public() and registered in HealthModule
- JwtAuthGuard extended with Reflector + @Public() bypass, registered as APP_GUARD
- LoggerModule is @Global(), exports LOGGER token, registered in AppModule
- CorrelationInterceptor registered as APP_INTERCEPTOR in AppModule
- NF-07 behavioral contract satisfied (401 on unauthenticated routes); RS256 signing deferred to Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` covering:
- Test results (pass/fail count, coverage percentages per module)
- List of files created in apps/api/src
- AppModule final imports/providers list
- Any deviations from the TDD plan and reasons
- Coverage exclusion list (auth/users excluded with justification)
- Note: JWT uses HS256 in Phase 1; RS256 upgrade is deferred to Phase 2
</output>
