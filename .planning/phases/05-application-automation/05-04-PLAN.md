---
phase: 05-application-automation
plan: "04"
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - apps/web/src/features/applications/EmailDraftModal.tsx
  - apps/web/src/features/applications/ApplicationDetailDrawer.tsx
  - apps/web/src/features/applications/StatusUpdateMenu.tsx
  - apps/web/src/features/history/ApplicationFilters.tsx
  - apps/web/src/features/history/CsvExportButton.tsx
  - apps/web/src/features/history/ApplicationHistoryPage.tsx
  - apps/web/src/features/profile/SmtpConfigSection.tsx
  - apps/web/src/features/profile/ProfilePage.tsx
  - apps/web/src/features/dashboard/PendingReviewQueue.tsx
  - apps/web/src/features/dashboard/DashboardPage.tsx
  - apps/web/src/components/AppLayout.tsx
  - apps/web/src/router.tsx
autonomous: false

requirements: [APPLY-02, APPLY-03, HIST-01, HIST-02, HIST-03]

must_haves:
  truths:
    - "User can click Apply by Email on a vacancy card and see a modal with Claude-generated draft"
    - "User can edit the email subject, body, and recipient before sending"
    - "User can send the email and see a success toast"
    - "User can view paginated application history with filters (status, company, platform, date)"
    - "User can click an application row and see full detail in a right-side drawer"
    - "User can update application status to tracking states via dropdown menu"
    - "User can export filtered applications as CSV"
    - "User can configure SMTP settings on the profile page"
    - "Dashboard shows pending review queue when drafts exist"
    - "Vacancy cards show Apply by Email button when emailDetectionMethod is not manual_required"
    - "Vacancy cards show application status badge when an application already exists for that vacancy"
  artifacts:
    - path: "apps/web/src/features/applications/EmailDraftModal.tsx"
      provides: "Email draft preview and send modal"
      contains: "EmailDraftModal"
    - path: "apps/web/src/features/applications/ApplicationDetailDrawer.tsx"
      provides: "Right-side drawer with full application detail"
      contains: "ApplicationDetailDrawer"
    - path: "apps/web/src/features/applications/StatusUpdateMenu.tsx"
      provides: "Dropdown menu for manual status updates"
      contains: "StatusUpdateMenu"
    - path: "apps/web/src/features/history/ApplicationFilters.tsx"
      provides: "Filter controls for application history"
      contains: "ApplicationFilters"
    - path: "apps/web/src/features/profile/SmtpConfigSection.tsx"
      provides: "SMTP configuration form section"
      contains: "SmtpConfigSection"
  key_links:
    - from: "apps/web/src/features/applications/EmailDraftModal.tsx"
      to: "/api/applications"
      via: "api.post('/applications')"
      pattern: "api\\.post.*applications"
    - from: "apps/web/src/features/history/ApplicationHistoryPage.tsx"
      to: "/api/applications"
      via: "api.get('/applications')"
      pattern: "api\\.get.*applications"
    - from: "apps/web/src/features/profile/SmtpConfigSection.tsx"
      to: "/api/users/smtp-config"
      via: "api.put('/users/smtp-config')"
      pattern: "api\\.put.*smtp-config"
    - from: "apps/web/src/features/dashboard/DashboardPage.tsx"
      to: "apps/web/src/features/applications/EmailDraftModal.tsx"
      via: "Apply by Email button on vacancy cards opens EmailDraftModal"
      pattern: "EmailDraftModal"
    - from: "apps/web/src/features/dashboard/DashboardPage.tsx"
      to: "/api/vacancies?includeApplication=true"
      via: "Dashboard fetches vacancies with application status join to show status badges on cards"
      pattern: "includeApplication.*true"
---

<objective>
Build all Phase 5 frontend components: EmailDraftModal, ApplicationDetailDrawer, StatusUpdateMenu, ApplicationFilters, CsvExportButton, SmtpConfigSection, PendingReviewQueue, and upgrade ApplicationHistoryPage with filters, pagination, and row-click detail. Add "Apply by Email" trigger button to vacancy cards in the Dashboard.

Purpose: Complete the user-facing application automation workflow -- users can configure SMTP, browse vacancies, trigger email draft generation, review/edit/send them, and track all applications with full history.
Output: 7 new components, 2 upgraded components, router and layout updates, vacancy card Apply button.
</objective>

<execution_context>
@C:/Users/1234/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1234/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-application-automation/05-CONTEXT.md
@.planning/phases/05-application-automation/05-UI-SPEC.md
@.planning/phases/05-application-automation/05-01-SUMMARY.md
@.planning/phases/05-application-automation/05-02-SUMMARY.md
@.planning/phases/05-application-automation/05-03-SUMMARY.md

<interfaces>
<!-- API endpoints from Plan 02 (ApplicationsController) -->
POST /applications { vacancyId, recipientEmail? } -> ApplicationDocument
GET /applications?page=N&status=X&company=X&platform=X&dateFrom=X&dateTo=X -> { data, total, page, pageSize }
GET /applications/:id -> { application, vacancy }
GET /applications/export/csv?status=X&dateFrom=X&dateTo=X -> CSV file
GET /applications/pending-count -> { count: number }
PATCH /applications/:id/status { status, note? } -> ApplicationDocument
PATCH /applications/:id/draft { emailContent: { subject, body }, recipientEmail? } -> ApplicationDocument
POST /applications/:id/send -> ApplicationDocument
POST /applications/:id/review -> ApplicationDocument

<!-- Vacancy application-status join from Plan 02 -->
GET /vacancies?includeApplication=true -> vacancy objects with extra `applicationStatus?: ApplicationStatus` field
  - applicationStatus is null when no application exists for that vacancy
  - applicationStatus is the current status string ('draft', 'sent', etc.) when an application exists

<!-- API endpoints from Plan 03 (UsersController SMTP) -->
PUT /users/smtp-config { host, port, secure?, user, password, fromName, fromEmail } -> { saved: true }
GET /users/smtp-config -> { host, port, secure, user, password: '********', fromName, fromEmail } | null

<!-- Vacancy fields from Plan 01 (email detection wired into persist path) -->
Vacancy documents now have:
- recipientEmail?: string -- detected email (null if not found)
- emailDetectionMethod?: 'apply_options' | 'jd_regex' | 'manual_required' -- how email was found

<!-- Frontend patterns from existing codebase -->
From apps/web/src/lib/api.ts:
```typescript
export const api = axios.create({ baseURL: API_BASE, withCredentials: true });
// All API calls must use this shared instance
```

From apps/web/src/features/history/ApplicationHistoryPage.tsx:
- Uses inline styles (not Tailwind classes -- project has Tailwind v4 but components use inline style objects)
- Color tokens: bg=#0f0f14, card=#1a1a24, border=#2a2a38, accent=#6366f1
- Typography: Inter font, 13px body, 11px labels, 24px headings, 28px display
- Status badge pattern: STATUS_BADGE_STYLES record with background/color/label
- ScorePill component with threshold-based colors
- StatCard component
- useQuery from @tanstack/react-query
- Inline SVG icons (no icon library)

From apps/web/src/features/profile/ProfilePage.tsx:
- Uses `api` from lib/api.ts for all API calls
- Inline styles following same token pattern
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SmtpConfigSection, ApplicationFilters, CsvExportButton, StatusUpdateMenu components</name>
  <files>
    apps/web/src/features/profile/SmtpConfigSection.tsx,
    apps/web/src/features/profile/ProfilePage.tsx,
    apps/web/src/features/history/ApplicationFilters.tsx,
    apps/web/src/features/history/CsvExportButton.tsx,
    apps/web/src/features/applications/StatusUpdateMenu.tsx
  </files>
  <read_first>
    apps/web/src/features/profile/ProfilePage.tsx,
    apps/web/src/features/history/ApplicationHistoryPage.tsx,
    apps/web/src/lib/api.ts,
    .planning/phases/05-application-automation/05-UI-SPEC.md,
    .planning/phases/05-application-automation/05-CONTEXT.md
  </read_first>
  <action>
    **All components MUST use inline style objects (NOT Tailwind classes) -- matching the existing pattern in ApplicationHistoryPage.tsx and ProfilePage.tsx.**

    **All API calls MUST use `api` from `../../lib/api.ts` -- never raw fetch or new axios instances.**

    **Color tokens to use consistently:**
    - Page bg: `#0f0f14`
    - Card bg: `#1a1a24`
    - Sidebar bg: `#13131c`
    - Primary border: `1px solid #2a2a38`
    - Subtle divider: `1px solid #1e1e2e`
    - Ghost border: `1px solid rgba(255,255,255,0.06)`
    - Accent: `#6366f1`
    - Destructive: `#ef4444`
    - Success: `#22c55e`
    - Warning: `#eab308`
    - Neutral: `#6b7280`
    - Body text: `#c8c8d8` or `#e2e2e8`
    - Muted text: `#6b7280`

    **Typography:**
    - Body: 13px, weight 400
    - Label: 11px, weight 600
    - Heading: 24px, weight 600, letterSpacing -0.04em
    - Display: 28px, weight 600, letterSpacing -0.04em, tabular-nums

    ---

    **1. Create `apps/web/src/features/profile/SmtpConfigSection.tsx`:**

    Section card with heading "Email Configuration", sub-heading "Required to send applications by email".

    Fields (all controlled inputs with inline styles):
    - Host (text input)
    - Port (number input, default 587)
    - Secure (toggle/checkbox, default false)
    - Username (text input)
    - Password (password input, masked) with note below: "Stored encrypted. For Gmail, use an App Password."
    - From Name (text input)
    - From Email (text input)

    On mount, call `api.get('/users/smtp-config')` to load existing config. Pre-fill fields if config exists. Password field shows empty (not ********) when editing.

    Save button: "Save Email Config" (accent fill `#6366f1`, white text). On click, call `api.put('/users/smtp-config', formData)`. Show inline success banner (green tinted) on 200: "Email configuration saved."

    Validation: Required fields (host, port, user, password, fromEmail) show `1px solid #ef4444` border on blur if empty when form submitted. Error text below field: 11px, `#ef4444`.

    Card styling: background `#1a1a24`, border `1px solid #2a2a38`, borderRadius `12px`, padding `24px`.

    **2. Integrate SmtpConfigSection into ProfilePage.tsx:**

    Import `SmtpConfigSection` and render it below the existing profile fields section. Add a visual separator (subtle divider `#1e1e2e`) between sections.

    **3. Create `apps/web/src/features/history/ApplicationFilters.tsx`:**

    Horizontal row of filter controls (flex, gap 12px). Wraps to column on mobile (`flexWrap: 'wrap'`).

    Filters:
    - Status: select dropdown with options: All, Draft, Pending Review, Sent, Tracking Active, Interview Scheduled, Offer Received, Rejected
    - Company: text input with placeholder "Filter by company..."
    - Platform: select dropdown with options: All, LinkedIn, Indeed, Computrabajo, etc.
    - Date range: two date inputs (From, To)

    Props interface:
    ```typescript
    interface ApplicationFiltersProps {
      filters: { status: string; company: string; platform: string; dateFrom: string; dateTo: string };
      onChange: (filters: ApplicationFiltersProps['filters']) => void;
      onClear: () => void;
    }
    ```

    "Clear Filters" text button (destructive color on hover) when any filter is active.

    Select/input styling: background `#13131c`, border `1px solid #2a2a38`, color `#e2e2e8`, borderRadius `8px`, padding `8px 12px`, fontSize `13px`.

    **4. Create `apps/web/src/features/history/CsvExportButton.tsx`:**

    Button "Export CSV" (ghost style: transparent bg, border `1px solid #2a2a38`, color `#c8c8d8`).

    Props: `filters: { status?: string; dateFrom?: string; dateTo?: string }`.

    On click:
    1. Set loading state (button shows "Exporting...")
    2. Call `api.get('/applications/export/csv', { params: filters, responseType: 'blob' })`
    3. Create a download link: `URL.createObjectURL(blob)`, set filename `applications-{YYYY-MM-DD}.csv`
    4. Trigger download, revoke URL
    5. Show "done" state briefly, then back to idle

    Error state: button text "Export failed" for 2s, then back to "Export CSV".

    **5. Create `apps/web/src/features/applications/StatusUpdateMenu.tsx`:**

    Inline dropdown (NOT a modal). Positioned below trigger button.

    Props:
    ```typescript
    interface StatusUpdateMenuProps {
      applicationId: string;
      currentStatus: string;
      onStatusUpdated: () => void;
      onClose: () => void;
    }
    ```

    Options (in order): Tracking Active, Interview Scheduled, Offer Received, Rejected.

    Each option has a colored dot indicator:
    - Tracking Active: `#6366f1` (accent)
    - Interview Scheduled: `#eab308` (warning)
    - Offer Received: `#22c55e` (success)
    - Rejected: `#ef4444` (destructive)

    On option select:
    1. Show note input field below options: placeholder "Add a note (optional)"
    2. "Save Status" confirm button (accent fill)
    3. "Never mind" cancel text link
    4. On confirm: call `api.patch(`/applications/${applicationId}/status`, { status, note })`
    5. Close menu and call `onStatusUpdated()`

    Menu styling: background `#1a1a24`, border `1px solid #2a2a38`, borderRadius `10px`, boxShadow `0 8px 32px rgba(0,0,0,0.4)`, padding `8px`.

    Accessibility: `role="menu"`, keyboard navigation (arrow keys, Enter to select, Escape to close).
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/features/profile/SmtpConfigSection.tsx contains `SmtpConfigSection`
    - apps/web/src/features/profile/SmtpConfigSection.tsx contains `api.put('/users/smtp-config'`
    - apps/web/src/features/profile/SmtpConfigSection.tsx contains `api.get('/users/smtp-config'`
    - apps/web/src/features/profile/SmtpConfigSection.tsx contains `Stored encrypted. For Gmail, use an App Password.`
    - apps/web/src/features/profile/SmtpConfigSection.tsx contains `Email Configuration`
    - apps/web/src/features/profile/SmtpConfigSection.tsx contains `Save Email Config`
    - apps/web/src/features/profile/ProfilePage.tsx contains `SmtpConfigSection`
    - apps/web/src/features/history/ApplicationFilters.tsx contains `ApplicationFilters`
    - apps/web/src/features/history/ApplicationFilters.tsx contains `status`
    - apps/web/src/features/history/ApplicationFilters.tsx contains `company`
    - apps/web/src/features/history/ApplicationFilters.tsx contains `Clear Filters`
    - apps/web/src/features/history/CsvExportButton.tsx contains `Export CSV`
    - apps/web/src/features/history/CsvExportButton.tsx contains `api.get('/applications/export/csv'`
    - apps/web/src/features/history/CsvExportButton.tsx contains `createObjectURL`
    - apps/web/src/features/applications/StatusUpdateMenu.tsx contains `StatusUpdateMenu`
    - apps/web/src/features/applications/StatusUpdateMenu.tsx contains `Save Status`
    - apps/web/src/features/applications/StatusUpdateMenu.tsx contains `Never mind`
    - apps/web/src/features/applications/StatusUpdateMenu.tsx contains `role="menu"`
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>SmtpConfigSection integrated into ProfilePage with save/load API calls and validation. ApplicationFilters provides status/company/platform/date filtering. CsvExportButton downloads filtered CSV. StatusUpdateMenu provides accessible dropdown with note input. All components use inline styles matching design tokens.</done>
</task>

<task type="auto">
  <name>Task 2: EmailDraftModal, ApplicationDetailDrawer, PendingReviewQueue, Apply by Email button on vacancy cards, and ApplicationHistoryPage upgrade</name>
  <files>
    apps/web/src/features/applications/EmailDraftModal.tsx,
    apps/web/src/features/applications/ApplicationDetailDrawer.tsx,
    apps/web/src/features/dashboard/PendingReviewQueue.tsx,
    apps/web/src/features/dashboard/DashboardPage.tsx,
    apps/web/src/features/history/ApplicationHistoryPage.tsx,
    apps/web/src/components/AppLayout.tsx,
    apps/web/src/router.tsx
  </files>
  <read_first>
    apps/web/src/features/history/ApplicationHistoryPage.tsx,
    apps/web/src/features/dashboard/DashboardPage.tsx,
    apps/web/src/components/AppLayout.tsx,
    apps/web/src/router.tsx,
    apps/web/src/lib/api.ts,
    .planning/phases/05-application-automation/05-UI-SPEC.md,
    .planning/phases/05-application-automation/05-CONTEXT.md,
    apps/web/src/features/applications/StatusUpdateMenu.tsx,
    apps/web/src/features/history/ApplicationFilters.tsx,
    apps/web/src/features/history/CsvExportButton.tsx
  </read_first>
  <action>
    **1. Create `apps/web/src/features/applications/EmailDraftModal.tsx`:**

    Full overlay modal per UI-SPEC: max-width 680px, centered, overlay rgba(0,0,0,0.6), card bg #1a1a24, border 1px solid #2a2a38, borderRadius 16px.

    Props:
    ```typescript
    interface EmailDraftModalProps {
      vacancyId: string;
      vacancyTitle: string;
      company: string;
      score: number;
      recipientEmail: string | null;
      onClose: () => void;
      onSent: () => void;
    }
    ```

    States per UI-SPEC:
    1. **Loading**: Spinner centered, text "Generating your personalized email..."
       - On mount, call `api.post('/applications', { vacancyId })` which creates draft with Claude-generated email
       - Then call `api.post(`/applications/${app._id}/review`)` to mark pending_review
    2. **Draft ready**: Two-panel layout (single column on mobile < 640px):
       - Left: editable subject input (single line, max 120 chars with counter) + body textarea (min-height 160px, max-height 320px, resize vertical, max 800 chars with counter)
       - Right: vacancy title + company + score pill (reuse ScorePill pattern)
       - Recipient email input pre-filled from vacancy, user can edit
       - Note below recipient: "You can edit the recipient address before sending."
    3. **Sending**: CTA shows spinner + "Sending...", disabled
       - Call `api.patch(`/applications/${app._id}/draft`, { emailContent, recipientEmail })` to save edits first
       - Then call `api.post(`/applications/${app._id}/send`)`
    4. **Sent**: Close modal, show toast "Email sent to [recipientEmail]." (simple fixed-position toast at top-right, auto-dismiss after 4s)
    5. **Error generate**: Inline banner inside modal: "Could not generate email draft. [error.message]. Try again or discard." with role="alert"
    6. **Error send**: Inline banner: "Failed to send email. Check your email configuration and try again." with role="alert"

    CTA placement: Bottom-right.
    - Primary: "Send Email" (accent fill #6366f1, white text)
    - Secondary: "Save Draft" (ghost: transparent bg, border #2a2a38) -- calls `api.patch(.../draft, ...)` then closes
    - Tertiary: "Discard" (text-only, #ef4444 on hover) -- just closes modal without saving

    Accessibility: `role="dialog"`, `aria-modal="true"`, focus trap while open, restore focus on close. `prefers-reduced-motion`: disable fade-in transition.

    Responsive: full-width on mobile < 640px (bottom-sheet pattern: borderRadius 16px top-only).

    **2. Create `apps/web/src/features/applications/ApplicationDetailDrawer.tsx`:**

    Right-side drawer, width 480px desktop, 100% mobile < 640px.

    Props:
    ```typescript
    interface ApplicationDetailDrawerProps {
      applicationId: string;
      onClose: () => void;
      onStatusUpdated: () => void;
    }
    ```

    On mount, call `api.get(`/applications/${applicationId}`)` -> returns `{ application, vacancy }`.

    Sections (top to bottom per UI-SPEC):
    1. **Header**: vacancy.title (heading 24px), vacancy.company (body muted), close button (X icon, `aria-label="Close application details"`)
    2. **Status timeline**: vertical list of `application.history[]` entries, most recent first. Each: StatusBadge + timestamp formatted + optional note text. Use semantic colors for each status badge.
    3. **Job details**: ScorePill(vacancy.compatibilityScore), platform badge, location, vacancy.url as external link `target="_blank" rel="noopener noreferrer"`
    4. **Email content** (if emailContent exists): collapsible section with heading "Email Sent". Subject line bold, body text in monospace-like card (bg #13131c, padding 16px, borderRadius 8px, fontFamily monospace).
    5. **Status update**: "Update Status" button opens StatusUpdateMenu inline below the button

    Accessibility: `role="dialog"`, `aria-modal="true"`, focus trap. Slide-in transition (200ms ease) disabled with `prefers-reduced-motion`.

    **3. Create `apps/web/src/features/dashboard/PendingReviewQueue.tsx`:**

    Horizontal scrollable card row. Hidden entirely when no drafts exist.

    On mount: call `api.get('/applications?status=draft&status=pending_review&page=1')` or use the dedicated `/applications/pending-count` endpoint + a separate query for the actual drafts.

    Card (260px width per UI-SPEC): company, title (truncated 1 line with text-overflow ellipsis), ScorePill, "Review Draft" CTA (ghost button, accent border).

    On "Review Draft" click: open EmailDraftModal with the application data (load full application via GET /applications/:id).

    Props:
    ```typescript
    interface PendingReviewQueueProps {
      onDraftReviewed: () => void; // callback to refresh parent
    }
    ```

    **4. Add "Apply by Email" trigger button to vacancy cards in DashboardPage.tsx:**

    This is the critical entry point for the email application flow. Without this button, users cannot initiate email applications from the vacancy browsing UI.

    **CRITICAL: Fetch vacancies with `?includeApplication=true`** -- Update the existing vacancy fetch call in DashboardPage.tsx to pass `includeApplication: 'true'` as a query parameter. This tells the backend (Plan 02's GET /vacancies extension) to join application status from the applications collection. Each vacancy object in the response will have an `applicationStatus?: string` field that is either null (no application) or the current status string ('draft', 'pending_review', 'sent', etc.).

    Read `apps/web/src/features/dashboard/DashboardPage.tsx` to find the vacancy card/list rendering. For each vacancy card:

    - **If vacancy already has an application (`vacancy.applicationStatus` is not null/undefined)**: Show the application status badge (reuse STATUS_BADGE_STYLES pattern) instead of any apply button. This prevents duplicate applications.
    - **If `vacancy.emailDetectionMethod` is NOT `'manual_required'` and vacancy has `recipientEmail`**: Render an "Apply by Email" button (accent ghost style: transparent bg, border `1px solid #6366f1`, color `#6366f1`, borderRadius 8px, padding `6px 12px`, fontSize 12px, fontWeight 600). On hover: bg `rgba(99,102,241,0.08)`.
    - **If `vacancy.emailDetectionMethod` is `'manual_required'` or `recipientEmail` is null**: Render an "Apply Manually" link/button (neutral ghost: border `#2a2a38`, color `#6b7280`) that opens `vacancy.url` in a new tab (`window.open(vacancy.url, '_blank')`).

    On "Apply by Email" click:
    1. Set state to show `EmailDraftModal` with props: `{ vacancyId: vacancy._id, vacancyTitle: vacancy.title, company: vacancy.company, score: vacancy.compatibilityScore, recipientEmail: vacancy.recipientEmail }`
    2. On modal `onSent` callback: refresh the vacancy list to show updated status

    Add `EmailDraftModal` import and state management (`selectedVacancy` state) to DashboardPage.tsx. Render the modal conditionally when `selectedVacancy` is set.

    **5. Upgrade `apps/web/src/features/history/ApplicationHistoryPage.tsx`:**

    Major rewrite:
    - Replace hardcoded `API_BASE_URL` with `api` from `../../lib/api.ts`
    - Replace `useQuery` fetch with `api.get('/applications', { params: filters })`
    - Add `ApplicationFilters` component above the table
    - Add `CsvExportButton` in the header area (next to page title)
    - Add pagination controls below the table (Previous/Next buttons, showing "Page X of Y")
    - Make table rows clickable -> open `ApplicationDetailDrawer` with the clicked applicationId
    - Update `ApplicationStatus` type and `STATUS_BADGE_STYLES` to include Phase 5 statuses:
      ```typescript
      const STATUS_BADGE_STYLES: Record<string, { background: string; color: string; label: string }> = {
        draft:                { background: 'rgba(107,114,128,0.12)', color: '#6b7280', label: 'Draft' },
        pending_review:       { background: 'rgba(99,102,241,0.12)',  color: '#818cf8', label: 'Pending Review' },
        sent:                 { background: 'rgba(34,197,94,0.12)',   color: '#22c55e', label: 'Sent' },
        tracking_active:      { background: 'rgba(99,102,241,0.12)',  color: '#6366f1', label: 'Tracking' },
        interview_scheduled:  { background: 'rgba(234,179,8,0.12)',   color: '#eab308', label: 'Interview' },
        offer_received:       { background: 'rgba(34,197,94,0.12)',   color: '#22c55e', label: 'Offer' },
        rejected:             { background: 'rgba(239,68,68,0.12)',   color: '#ef4444', label: 'Rejected' },
        // Legacy statuses for backward compat
        applied:              { background: 'rgba(34,197,94,0.12)',   color: '#22c55e', label: 'Applied' },
        failed:               { background: 'rgba(239,68,68,0.12)',   color: '#ef4444', label: 'Failed' },
        skipped_low_score:    { background: 'rgba(234,179,8,0.12)',   color: '#eab308', label: 'Skipped' },
        already_applied:      { background: 'rgba(107,114,128,0.12)', color: '#6b7280', label: 'Already Applied' },
        easy_apply_not_available: { background: 'rgba(107,114,128,0.12)', color: '#6b7280', label: 'No Easy Apply' },
      };
      ```
    - Add filtered empty state: "No results for these filters" / "Try clearing some filters to see more applications."
    - Row hover: `cursor: 'pointer'` to indicate clickability
    - Remove the standalone page wrapper (bg, font) since the page is rendered inside AppLayout

    **6. Update `apps/web/src/components/AppLayout.tsx`:**

    Add "Applications" nav item to the sidebar navigation list. Use an inline SVG icon (mail/paper-plane style). The nav item should show a badge with the pending review count (from `api.get('/applications/pending-count')`). Badge styling: accent bg rgba(99,102,241,0.12), text #818cf8, 11px, borderRadius 999px.

    **7. Update `apps/web/src/router.tsx`:**

    Ensure the `/applications` or `/history` route renders the updated `ApplicationHistoryPage`. If a route doesn't exist for it, add one. The route should be within the authenticated layout.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/features/applications/EmailDraftModal.tsx contains `EmailDraftModal`
    - apps/web/src/features/applications/EmailDraftModal.tsx contains `role="dialog"`
    - apps/web/src/features/applications/EmailDraftModal.tsx contains `aria-modal="true"`
    - apps/web/src/features/applications/EmailDraftModal.tsx contains `Generating your personalized email`
    - apps/web/src/features/applications/EmailDraftModal.tsx contains `Send Email`
    - apps/web/src/features/applications/EmailDraftModal.tsx contains `Save Draft`
    - apps/web/src/features/applications/EmailDraftModal.tsx contains `Discard`
    - apps/web/src/features/applications/EmailDraftModal.tsx contains `api.post('/applications'`
    - apps/web/src/features/applications/ApplicationDetailDrawer.tsx contains `ApplicationDetailDrawer`
    - apps/web/src/features/applications/ApplicationDetailDrawer.tsx contains `role="dialog"`
    - apps/web/src/features/applications/ApplicationDetailDrawer.tsx contains `aria-label="Close application details"`
    - apps/web/src/features/applications/ApplicationDetailDrawer.tsx contains `StatusUpdateMenu`
    - apps/web/src/features/applications/ApplicationDetailDrawer.tsx contains `Email Sent`
    - apps/web/src/features/dashboard/PendingReviewQueue.tsx contains `PendingReviewQueue`
    - apps/web/src/features/dashboard/PendingReviewQueue.tsx contains `Review Draft`
    - apps/web/src/features/dashboard/DashboardPage.tsx contains `Apply by Email`
    - apps/web/src/features/dashboard/DashboardPage.tsx contains `Apply Manually`
    - apps/web/src/features/dashboard/DashboardPage.tsx contains `EmailDraftModal`
    - apps/web/src/features/dashboard/DashboardPage.tsx contains `emailDetectionMethod`
    - apps/web/src/features/dashboard/DashboardPage.tsx contains `includeApplication`
    - apps/web/src/features/dashboard/DashboardPage.tsx contains `applicationStatus`
    - apps/web/src/features/history/ApplicationHistoryPage.tsx contains `ApplicationFilters`
    - apps/web/src/features/history/ApplicationHistoryPage.tsx contains `CsvExportButton`
    - apps/web/src/features/history/ApplicationHistoryPage.tsx contains `ApplicationDetailDrawer`
    - apps/web/src/features/history/ApplicationHistoryPage.tsx contains `api.get('/applications'`
    - apps/web/src/features/history/ApplicationHistoryPage.tsx contains `pending_review`
    - apps/web/src/components/AppLayout.tsx contains `Applications` or `applications`
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>All Phase 5 frontend components built and integrated. EmailDraftModal handles full generate-edit-send flow. ApplicationDetailDrawer shows full detail with status timeline and email content. StatusUpdateMenu provides manual tracking updates. DashboardPage fetches vacancies with ?includeApplication=true to get application status, vacancy cards show status badge when application exists, "Apply by Email" button (when email detected) or "Apply Manually" link (when manual_required). ApplicationHistoryPage upgraded with filters, pagination, row-click detail, CSV export. SmtpConfigSection integrated into ProfilePage. PendingReviewQueue shows on Dashboard. AppLayout has Applications nav item with pending count badge.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete Phase 5 workflow</name>
  <what-built>Complete Phase 5 application automation workflow: SMTP configuration, email draft generation via Claude API, draft review/edit/send modal, application history with filters/pagination/detail/CSV export, manual status tracking, pending review queue on Dashboard, Apply by Email trigger button on vacancy cards.</what-built>
  <how-to-verify>
    1. Start the API: `cd apps/api && npm run start:dev`
    2. Start the web app: `cd apps/web && npm run dev`
    3. Log in and navigate to Profile page -- verify "Email Configuration" section appears below profile fields
    4. Fill in SMTP config (use a test Gmail account with app password) and click "Save Email Config" -- verify success banner appears
    5. Navigate to Dashboard -- verify scored vacancies from Phase 4 appear with "Apply by Email" buttons on cards where email was detected
    6. Verify vacancies with `emailDetectionMethod: 'manual_required'` show "Apply Manually" link instead
    7. Verify vacancies that already have an application show a status badge instead of any apply button
    8. Click "Apply by Email" on a vacancy -- verify EmailDraftModal opens with loading spinner, then shows generated email draft
    9. Edit the subject and body text, verify character counters update
    10. Click "Send Email" -- verify toast "Email sent to [email]" appears (requires valid SMTP config)
    11. Return to Dashboard -- verify the vacancy now shows "Sent" status badge instead of "Apply by Email" button
    12. Navigate to Application History page -- verify the sent application appears in the table
    13. Click the application row -- verify ApplicationDetailDrawer slides in from right showing full detail (job description, score, email content, status timeline)
    14. Click "Update Status" in the drawer -- verify StatusUpdateMenu appears with options (Tracking Active, Interview Scheduled, Offer Received, Rejected)
    15. Select a status, add optional note, click "Save Status" -- verify timeline updates
    16. Use filters (status dropdown, company search) -- verify table filters correctly
    17. Click "Export CSV" -- verify CSV file downloads with correct data
    18. Verify all components work on mobile viewport (360px width)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `cd apps/web && npx tsc --noEmit` exits 0
2. All 7 new component files exist in their respective directories
3. ApplicationHistoryPage.tsx uses `api` import (not raw fetch)
4. AppLayout.tsx contains "Applications" nav item
5. DashboardPage.tsx contains "Apply by Email" button rendering with emailDetectionMethod check
6. DashboardPage.tsx imports and conditionally renders EmailDraftModal
7. DashboardPage.tsx passes `includeApplication: 'true'` when fetching vacancies
8. DashboardPage.tsx checks `vacancy.applicationStatus` before rendering apply buttons
9. Manual verification of full workflow (SMTP config -> browse vacancies -> apply by email -> draft -> review -> send -> history -> status update)
</verification>

<success_criteria>
- User can configure SMTP on profile page
- User can click "Apply by Email" on vacancy cards in the Dashboard to trigger email draft generation
- User can generate, review, edit, and send email applications
- User can view paginated application history with filters
- User can see full application detail in drawer
- User can manually update application status
- User can export applications as CSV
- Dashboard shows pending review queue
- Vacancy cards show appropriate button based on emailDetectionMethod
- Vacancy cards show application status badge when an application already exists
- Dashboard fetches vacancies with ?includeApplication=true to populate applicationStatus
- All components follow the inline style pattern with correct design tokens
- All components are accessible (role, aria-label, keyboard navigation, focus management)
</success_criteria>

<output>
After completion, create `.planning/phases/05-application-automation/05-04-SUMMARY.md`
</output>
